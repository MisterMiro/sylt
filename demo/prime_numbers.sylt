## code for checking whether a number is prime

let isPrime(n) = do
    let check(n i) = if n is i: true
        else if n % i is 0: false
        else check(n i + 1)

    if n < 2: false else check(n 2)
end

let numbers = List.range(0 100)
numbers <- List.filter(isPrime numbers)
printLn(numbers)
