## this program generates a string containing
## a random series of letters and digits

let length = 24

let str = ""
rep(fun(_) -> {
	let n = Math.rand(0, List.length(chars))
	str <- str ++ chars[n]
}, length)

printLn(str)
